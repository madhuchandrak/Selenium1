class Locators():

    # Login page locators
    username_textbox_id = "txtUsername"  # object name = username, object name type = textbox, locator type = id
    password_textbox_id = "txtPassword"  # object name = password, object name type = textbox, locator type = id
    login_button_id = "btnLogin"  # object name = button, object name type = button, locator type = id

    # Home page locators
    welcome_link_id = "welcome"  # object name = welcome, object name type = link, locator type = id property
    logout_link_linkText = "Logout"  # object name = logout, object name type = link, locator type = link text property